import 'package:flutter/material.dart';

class StatScreenTab extends StatelessWidget {
  const StatScreenTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("statistic screen tab "),
    );
  }
}
