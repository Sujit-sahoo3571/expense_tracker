import 'package:flutter/material.dart';

class WalletScreenTab extends StatelessWidget {
  const WalletScreenTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Wallet Screen Tab "),
    );
  }
}
